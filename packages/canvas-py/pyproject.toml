[project]
name = "langchain-canvas"
version = "0.1.0"
description = "Turn a LangChain agent's tools into a live React canvas."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "langchain>=1.0,<2.0",
    "langgraph>=1.0",
    "pydantic>=2.7",
]

[project.optional-dependencies]
xlsx = [
    "openpyxl>=3.1",
]
pdf = [
    "pypdf>=4",
]
office = [
    "python-docx>=1.1",
    "python-pptx>=0.6.23",
]
ingestion = [
    "openpyxl>=3.1",
    "pypdf>=4",
    "python-docx>=1.1",
    "python-pptx>=0.6.23",
]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "ruff>=0.6",
    "mypy>=1.10",
    "openpyxl>=3.1",
    "pypdf>=4",
    "python-docx>=1.1",
    "python-pptx>=0.6.23",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/langchain_canvas"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
